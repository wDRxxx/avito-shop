run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  disable-all: true
  enable:
    - gocyclo
    - errcheck
    - unused
    - sloglint
    - goconst
    - unconvert
    - gocritic
    - staticcheck
    - gosec
    - govet
    - gofmt

linters-settings:
  gofmt:
    simplify: true